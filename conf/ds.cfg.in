#-- The port for the catalog to run XMPP on
XMPP_PORT = 5222

#-- The port for the catalog to run HTTP on
HTTP_PORT = 8080

#-- The password to use to connect to data storage
DSADMIN_PASS = your-password-here
